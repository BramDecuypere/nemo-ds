{
  "name": "nemo-ds-monorepo",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "description": "Nemo design system monorepo (Stencil component library + Angular integration)",
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^9.0.7",
    "typescript": "^6.0.3"
  },
  "license": "MIT"
}
